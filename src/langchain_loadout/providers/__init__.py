"""Adapters that satisfy the `Judge` port for a specific provider.

Each adapter lives in its own module and brings its own optional dependency, so importing this
package costs nothing until a specific adapter is imported.
"""
